In this directory are all the systems shared by each of the Train Engine's
subsystems. This includes the YAML parser, Component Database, and Scene Graph.
These systems are compiled into a shared library, which is then used by the
individual subsystems.

